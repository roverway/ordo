# CLAUDE.md

减少常见 LLM 编码错误的行为准则。可根据项目特定说明进行合并。

**权衡：** 这些准则偏向谨慎而非速度。对于琐碎任务，自行判断。

## 1. 先思考，再编码

**不要假设。不要隐藏困惑。暴露权衡。**

在实现之前：
- 明确陈述你的假设。如果不确定，要问。
- 如果存在多种解释，提出来——不要默默选择。
- 如果存在更简单的方法，说出来。在必要时提出反对意见。
- 如果某些内容不清楚，停下来。说出令人困惑的地方。提问。

## 2. 简洁优先

**用最少的代码解决问题。不做任何推测性的工作。**

- 不做需求之外的功能。
- 不为一次性代码做抽象。
- 不提供未被要求的"灵活性"或"可配置性"。
- 不为不可能发生的场景做错误处理。
- 如果你写了 200 行代码但它本可以用 50 行完成，重写它。

问问自己："资深工程师会觉得这过于复杂吗？"如果是，就简化。

## 3. 手术刀式修改

**只动你必须动的。只清理你自己的烂摊子。**

在编辑现有代码时：
- 不要"改进"相邻的代码、注释或格式。
- 不要重构没有坏的东西。
- 匹配现有风格，即使你会有不同做法。
- 如果你注意到不相关的死代码，提出来——不要删除它。

当你的修改产生了废弃内容：
- 移除因**你的修改**而变得无用的 import/变量/函数。
- 除非被要求，否则不要移除原有的死代码。

检验标准：每一行修改都应该直接追溯到用户的需求。

## 4. 目标驱动执行

**定义成功标准。循环直到验证通过。**

将任务转化为可验证的目标：
- "添加验证" → "为无效输入写测试，然后让它们通过"
- "修复 bug" → "写一个能复现它的测试，然后让它通过"
- "重构 X" → "确保重构前后测试都能通过"

对于多步骤任务，简要说明计划：
```
1. [步骤] → 验证：[检查点]
2. [步骤] → 验证：[检查点]
3. [步骤] → 验证：[检查点]
```

## 5. 自定义工程质量规范

高于一切的总体原则：**本项目的维护者不是程序员。所有代码必须是合格的职业工程师水平——可维护、可扩展、低耦合、完整测试覆盖、有清晰的边界与一致的风格。交付即默认可长期运行。**

### 运行环境
	- 如果是windows系统，则使用系统原生powershell以及scoop安装的git、python、nodes等工具；
	- 如果是linux系统，则使用系统包管理器安装的git、python、nodes等工具；
	- 如果使用Python，务必使用uv，不要破坏系统自带Python环境
  - 对于其他变成语言（例如nodejs），如有可能，都使用虚拟环境，不破坏本地环境。
  - 如果遇到网络问题，可尝试使用代理服务器127.0.0.1:10808

### 强制中文
- 模型思考和沟通都必须使用中文。
- 所有代码注释、提交信息、技术讨论均用中文。


### 测试规范

- 每个功能模块必须提供对应的测试文件（`foo.py` → `test_foo.py`，`bar.ts` → `bar.test.ts`）
- 测试分层：
  - **单元测试**：每个纯逻辑函数必须具备。边界情况（空输入、极端值、异常路径）必须覆盖。
  - **集成测试**：跨模块协作路径至少一条 happy path。
  - **端到端测试**（可选，待网站成型后）：关键用户交互流程（加载页面 → 浏览 → 下载）。
- 覆盖率要求：核心逻辑 ≥ 80%。不含存根和配置文件的纯覆盖率数字无意义——关注分支覆盖和边界条件覆盖。
- 测试必须能在本地单命令运行（`npm test` / `python -m pytest`）。
- 禁止通过删除测试来让 CI 通过。

### 模块化与架构原则

- **单一职责**：一个模块做一件事。文件名说明它是什么，而非它被谁调用。
- **关注点分离**：数据获取、数据转换、视图渲染三者分属不同文件和目录。禁止在 UI 组件内部直接发起网络请求。
- **依赖方向**：依赖从上到下（UI → 逻辑 → 数据），禁止反向引用。出现循环依赖时，提取共同抽象到独立模块。
- **接口先于实现**：所有模块对外暴露明确的公共 API 签名（函数签名、组件 Props）。隐藏实现细节。修改内部实现不应导致调用方改动。
- **目录结构扁平优先**：三层以内。深度超过四层的嵌套目录通常意味着过度拆分或职责不清，应重新审视设计。

### 版本控制规范

- 及时提交：完成一次开发后及时自动提交，不要积攒很多修改后才提交
- **提交粒度**：一次提交完成一件独立的事情。禁止"修 bug + 重构 + 新增功能"混在一起提交。
- **提交信息格式**：`类型: 简要描述`，**使用中文**。
  - 类型示例：`feat`（新功能）、`fix`（修 bug）、`refactor`（重构）、`docs`（文档）、`chore`（工具/配置）
  - 示例：`feat: 添加今日最新壁纸模块`、`fix: 修复图片懒加载在 Safari 下的崩溃`
- **分支策略**：`main` 为上线分支，功能开发在 `feature/xxx` 分支，修复在 `fix/xxx` 分支。
- **严禁**：force push、跳过 hook、提交密钥/密码/API Key。

### 代码审查的必要条件

每次提交到仓库的代码必须满足以下全部条件，否则视为不合格：

1. 类型检查零错误
2. 格式化零差异
3. 静态检查零警告
4. 所有现有测试通过，新增逻辑有对应测试
5. 不存在已知安全漏洞（无硬编码密钥、无 XSS 漏洞、无 SQL 注入等）
6. 不存在明显性能问题（如循环内重复查询、未做懒加载的大量图片请求）

### 变更影响范围控制

- 每次变更前，先列出可能受影响的文件和功能，评估回归风险。
- 修改共享模块（工具函数、通用组件、配置）时检查所有调用方，确保兼容。
- 不允许「顺手」扩大改动范围——修 bug 就是修 bug，重构是重构，不要混在一起。

### 文档管理规范

- **计划必文档化**：每一次大的实施计划（涉及多文件、多步骤、架构决策）必须在执行前写入独立文档。不要停留在对话记录中。
- **结果必记录**：实施完成后，在实际的文档中追加执行结果（做了什么、实际耗时、遇到的问题、未完成项）。
- **文件命名规则**：所有文档使用 `YYYYMMDD_HHMM_简短描述.md` 格式（如 `20260522_1829_项目实施计划.md`），时间戳精确到分钟且在前，便于按时间排序和追溯。
- **存放位置**：所有规划与实施记录文档统一存放于 `docs/` 目录。
- **禁止行为**：不接受「计划在脑子里」就直接写代码。先写文档，确认无误后再动手。

